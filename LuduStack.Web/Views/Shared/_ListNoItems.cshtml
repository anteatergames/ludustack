@model ListNoItemsViewModel

<div class="text-center p-4 bg-white @(String.IsNullOrWhiteSpace(Model.MainClass) ? "noitems" : Model.MainClass)">
    <i class="fa-8x @Model.Icon noitems-icon @(Model.Rotate ? string.Empty : "norotate")" aria-hidden="true"></i>
    <div class="mt-4">
        <span>@SharedLocalizer[Model.Text]</span>
    </div>
</div>